#include <stdio.h>
#include<string.h>
int main()
{
    char name[100];
    // printf("enter numbr of element:");
    // scanf("%s", &element);
    // for (i = 0; i < element; i++)
    // {
    printf("enter the student name:"); /* code */
    gets(name);
    // }
    printf("entered name is:%s", name);
}